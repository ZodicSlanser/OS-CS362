apiVersion: v1
kind : Pod
metadata :
  name : pod1
spec :
  containers :
    - image : zodicslanser/simpleserver
      name : img1
      ports:
        - containerPort: 80
      resources :
         limits :
           memory : "512Mi"
           cpu : "1"    

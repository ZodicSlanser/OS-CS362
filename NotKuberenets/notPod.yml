apiVersion: v1
kind : Pod
metadata :
  name : pod1
spec :
  containers :
    - image : nginx:alpine
      name : img1
      resources :
         limits :
           memory : "512Mi"
           cpu : "1"
  

    
